import { el } from '../utils/dom.js';import { getState } from '../state.js';import { getRole } from '../permissions.js';export const Home=(mount)=>{ const s=getState(); const role=getRole(); mount.replaceChildren(el('section',{className:'main-card'},[ el('h2',{},['Inicio']), el('p',{className:'text-muted mt-1'},['Bienvenido. Esta vista aparece después de iniciar sesión.']), el('p',{className:'mt-1'},[`Tema actual: ${s.theme}`]), el('p',{className:'mt-1'},[`Rol actual: ${role || 'pendiente de asignación'}`]) ])); };